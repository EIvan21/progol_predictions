[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    slow: end-to-end tests that train a model (deselect with -m "not slow")
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning
    ignore::UserWarning
